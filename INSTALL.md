How to run (REQUIRES PYTHON 3.11):

1. python -m venv .venv
2. py -3.11 -m venv .venv
3. .\.venv\Scripts\Activate.ps1
4. python -m pip install --upgrade pip
5. python tts_cli_player_basic.py (can use the v2 one for clearer dialogue, save one for it to save directly)